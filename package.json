{
  "name": "mojave-run",
  "version": "3.0.1",
  "private": true,
  "description": "MOJAVE RUN — multiplayer relay + static host",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "stripe": "^22.1.1",
    "ws": "^8.18.0"
  }
}
