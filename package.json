{
  "name": "mojave-run",
  "version": "1.0.0",
  "private": true,
  "description": "MOJAVE RUN — multiplayer relay + static host",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
